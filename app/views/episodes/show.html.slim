h3 Title:
p = @episode.title

h3 Description:
p = @episode.description

h4 Tags
p = render @episode.tags

- if current_user_subscribed?
  p SECRET VIDEO HERE
-else
  = link_to 'Subscribe to watch', new_subscription_path, class: 'btn btn-primary'
